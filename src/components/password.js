import React from "react";

export default class Password extends React.Component {
    render() {
        return (
            <input className="container" type='text' placeholder="Password"></input>
        )
    }
}